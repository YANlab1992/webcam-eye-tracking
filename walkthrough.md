# 眼动追踪系统算法升级与监控交互优化说明 (Walkthrough)

针对用户反馈的测试偏差与交互问题：
1. **“眼动调点过于频繁，太过灵敏”**（高频锯齿与跳跃）；
2. **“眼动边界往左和往右都很难达到”**（左右物理幅度受限）；
3. **“右上角呈现面部整合情况会影响九点校准，要有隐藏按钮”**（遮挡标定点 3 号）。

本次已全面排查根因并完成彻底修复与重构。遵循要求，**未启动任何浏览器或自动化网页测试**，所有修改均已本地生效，供被试自行在浏览器中刷新测试。

---

## 一、核心问题根因排查与修复方案

### 1. 右上角监控视窗遮挡九点校准第 3 号标定点
- **原因剖析**：右上角的 `#cam-monitor-widget` 固定定位在 `top: 74px, right: 20px, 宽 280px`，而九点校准中的第 3 号标定点位于 `top: 12%, left: 90%`（约距离右边界 192px），被监控视窗完全遮盖，导致被试无法注视和点击该点。
- **重构实现**：
  - 在监控视窗标题栏新增 **【✕ 隐藏】** 按钮。
  - 隐藏后自动浮现极简常驻胶囊按钮 **【📷 展开面部监控】**（位于右上角微缩区域，仅 28px 高，绝不遮挡任何标定点），点击即可随时重新唤出。
  - 在顶部状态栏新增快捷开关 **【📷 隐藏/展开监控】**。
  - **九点校准阶段自动隐藏**：在进入 `stage-calibration` 时，系统自动触发折叠隐藏，确保第 3 号标定点 100% 暴露且点击畅通无阻；退出校准后智能恢复。

---

### 2. 根除高频跳点与超灵敏锯齿（抗噪平滑滤波重构）
- **原因剖析**：
  - 原算法在计算瞳孔中心列时使用离散整数列的 `argmax`。在 25 像素左右的眼裂切片中，CMOS 噪点引起 1 个像素的离散跳动即产生 4% 的特征位移，映射到屏幕上就是 90~180px 的剧烈阶跃。
  - 原平滑滤波器在单帧位移超过 32px 时将响应因子 $\alpha$ 猛增至 0.88，导致噪点被误当成快速扫视而放大，形成密集的锯齿状心电图轨迹。
- **重构实现**：
  - **亚像素加权质心解算（Center-of-Mass Sub-pixel Centroid）**：在瞳孔暗度列积分极值邻域内，进行亚像素连续质心与二次抛物线插值，输出高精度浮点坐标（如 13.42 像素），消灭离散阶跃。
  - **特征空间低通预滤波（EMA Filter）**：在特征级先行过滤 CMOS 噪点，阻断噪点进入大比例尺坐标变换。
  - **5 帧中值滤波 + 速度自适应阻尼滤波**：滑动窗口由 3 帧扩充至 5 帧中值，过滤单帧飞点；对单帧位移超过 160px 的野值进行软限幅；在平滑追随区间将平滑系数控制在最舒适的阻尼曲线（即使在大扫视区也将上限钳制在 0.52，保留 48% 阻尼防止超调振荡）。
  - **顶部新增【🛡️ 滤波降噪】滑块**：提供 1~5 档（极速跟随 / 轻度平滑 / 平衡(推荐) / 稳健抗抖 / 超强稳态消抖），方便用户依据环境光线与摄像头噪点按需微调。

---

### 3. 水平眼动边界轻快可达（左右边界拓展与非线性动力学）
- **原因剖析**：
  - 人眼在屏幕前自然转动注视左右两端（约 $\pm 12^\circ$ 视角）时，瞳孔水平归一化偏移量通常仅为 $\pm 0.08 \sim \pm 0.12$。
  - 原水平基准增益仅有 3.6x，计算出的归一化跨度仅约 $\pm 0.35$，光标被物理截断在中心 35% 范围内，根本无法到达左右两端 12% 目标区域。
- **重构实现**：
  - **水平基准增益倍增**：基准水平增益由 3.6x 提升至 6.8x。
  - **非线性边缘幂律拓展（Nonlinear Boundary Expansion）**：引入公式 $f(x) = \text{sgn}(x) \cdot (|x| + 1.8 \cdot |x|^{1.35})$。中心注视区保持平稳线性、不放大微震；一旦视线偏向外周两翼，增益平滑加速，使眼球无需费力瞪眼即可轻松覆盖 `0.05W ~ 0.95W` 全幅范围。
  - **顶部新增【↔️ 水平范围】滑块**：默认 2.2x，支持 1.0x ~ 4.0x 连续滑动微调。
  - **九点校准中心点 5 自动基准锁死**：标定过程中当被试点击中心 5 号点时，自动提取多帧均值将基准锁定于竞技场中心，杜绝初始单侧漂移。

---

## 二、修改文件一览

- [`index.html`](file:///C:/Users/yanAndzeng/.gemini/antigravity-ide/scratch/webcam_eye_tracking_demo/index.html)：顶部新增【↔️ 水平范围】、【🛡️ 滤波降噪】滑块及【📷 监控视窗】开关；监控窗头部新增【✕ 隐藏】按钮与浮动唤出胶囊。
- [`style.css`](file:///C:/Users/yanAndzeng/.gemini/antigravity-ide/scratch/webcam_eye_tracking_demo/style.css)：新增 `.hidden-monitor` 隐藏过渡、`.cam-monitor-floating-pill` 悬浮胶囊按钮及高对比度控制组件样式。
- [`app.js`](file:///C:/Users/yanAndzeng/.gemini/antigravity-ide/scratch/webcam_eye_tracking_demo/app.js)：
  - 实现亚像素加权质心解算与抛物线插值；
  - 实现特征域 EMA 低通滤波与 5 帧中值软限幅自适应平滑；
  - 实现非线性边缘幂律拓展与左右边界增益提升；
  - 实现九点校准自动折叠监控视窗与中心点基准自锁；
  - 绑定新增滑块与显隐按钮事件。

---

## 三、用户本地测试指引（请在本地浏览器中自行刷新）

1. **测试监控窗隐藏与九点校准**：
   - 刷新网页后，可点击右上角监控视窗标题栏的 **【✕ 隐藏】**，监控窗收起并显示右上方极简胶囊 **【📷 展开面部监控】**。
   - 点击 **【🎯 9 点校准 + 独立验证】**，系统会自动折叠监控视窗，查看右上角 **第 3 号标定点** 是否清晰可见、没有任何遮挡，并顺利完成 9 点标定。
2. **测试左右边界与平滑度**：
   - 进入 **【从左到右】** 几何追踪任务，端坐注视小球从左至右移动：
   - 观察视线光标是否能够轻快、自如地到达左侧（12% W）与右侧（88% W）目标球位置；
   - 观察青蓝色视线轨迹是否呈现平滑丝滑线条，彻底告别密集心电图锯齿；
   - 若您环境光线噪点较多，可直接在顶部微调 **【🛡️ 滤波降噪】** 至“稳健抗抖”或“超强稳态消抖”；若需要更大左右跨度，可顺调 **【↔️ 水平范围】** 滑块。
